# spam-detector
SMS Spam Classifier Web App built using Python, NLP, and Machine Learning. The model uses TF-IDF vectorization and Multinomial Naive Bayes to classify messages as Spam or Ham. Includes text preprocessing (tokenization, stopword removal, stemming) and a Streamlit web interface deployed for real-time predictions.
